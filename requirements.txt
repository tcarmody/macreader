# Core
fastapi>=0.127.0
uvicorn>=0.40.0
python-dotenv>=1.2.1
pydantic>=2.12.0
slowapi>=0.1.9          # Rate limiting

# HTTP & Parsing
aiohttp>=3.13.0
Brotli>=1.1.0           # Brotli content-encoding support
feedparser>=6.0.12
beautifulsoup4>=4.14.0
lxml>=6.0.0
trafilatura>=2.0.0      # Reader-mode article extraction

# Document Extraction
pymupdf>=1.25.0       # PDF text extraction
python-docx>=1.1.0    # DOCX text extraction
python-multipart>=0.0.20  # File uploads

# AI Providers (install at least one)
anthropic>=0.75.0       # Recommended - supports prompt caching
openai>=1.60.0          # Alternative provider
google-genai>=1.56.0        # Alternative provider

# Advanced Features (optional)
# Install Playwright for JS rendering: pip install playwright && playwright install chromium
playwright>=1.49.0

# Development
pytest>=9.0.0
pytest-asyncio>=1.3.0
